#include "main.h"
/**
 * _abs - computes absolute value of an interger
 * @r: the interger to compute
 * Return: the absolute value of n
 */

int _abs(int)
{
	if (r < 0)
		_putchar('-')
	return(r < 0 ? -r : r);
}
