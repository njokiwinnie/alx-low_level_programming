#include "main.h"
#include <stdio>
/**
 * add -adds two integers and returns result
 * @a: first interger.
 * @b: second integer.
 * Return: reuslt after the addition
 */
int add(int a, int b)
{
	return (a + b);
}
