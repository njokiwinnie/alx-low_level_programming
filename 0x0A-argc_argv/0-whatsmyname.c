#include <stdio.h>
/**
 * main - function
 * @argc: number of commands
 * @argv: pointer array
 * Return: success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
